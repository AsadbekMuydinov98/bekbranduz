import styled from "styled-components";

export const HomeWrapper = styled.div`
  padding: 20px;

  .features-section,
  .news-section,
  .products-section {
    margin-bottom: 40px;
  }

  .section-title {
    text-align: center;
    margin: 40px 0 20px;
    font-size: 24px;
    font-weight: bold;
  }
`